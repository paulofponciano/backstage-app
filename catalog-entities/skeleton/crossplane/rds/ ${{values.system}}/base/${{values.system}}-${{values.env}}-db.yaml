# CLAIM
---
apiVersion: api.pauloponciano.digital/v1alpha1
kind: CustomDatabase
metadata:
  name: ${{values.name}}-${{values.env}}-db
  namespace: environment-crossplane
spec:
  resourceConfig:
    providerConfigName: aws
    region: ${{values.database.properties.region}}
    size: ${{values.database.properties.size}}
    engine: ${{values.database.properties.engine}}
    version: ${{values.database.properties.version}}
    tags:
      automation-by: crossplane
      ownerName: ${{values.owner}}


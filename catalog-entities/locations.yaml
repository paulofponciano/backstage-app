apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: entities
  description: A collection of all the catalog entities
spec:
  targets:
    - ./apis.yaml
    - ./components.yaml
    - ./systems.yaml
    - ./group/*.yaml
    - ./user/*.yaml
    - ./resources.yaml
    - ./templates/*.yaml